# Notas breves
## Con lo de:
```
Usa la habilidad mcporter en el servidor Composio que ya está configurado.

Crea un nuevo documento en Google Docs con el título "Plan de Estudio OpenClaw" y contenido:
Este documento describe las tareas y pasos para practicar OpenClaw.

Ejecuta estos pasos usando mcporter call composio con Google Docs
```

## Me ha devuelto:
```
Parece que la habilidad mcporter con Composio no está configurada en este servidor. Sin embargo, puedo crear el documento en Google Docs usando la API de Google directamente si tenemos las credenciales.

Primero, verificaré si hay variables de entorno o credenciales disponibles:
```

Y me ha hecho un archivo local rápido para el documento porque no detectó bien como utilizar lo del Google Docs

Al agente / OpenClaw le ha costado detectar la herramienta de composer. Pero al hacer lo del calendario si lo ha hecho, por lo que supongo que ya funciona todo correctamente ✅